#version 330 core
out vec4 FragColor;
  
//in vec3 ourColor;
in vec2 texCoord;

uniform sampler2D firstTexture;
uniform sampler2D secondTexture;

void main() {
    FragColor = mix(texture(firstTexture, texCoord), texture(secondTexture, texCoord), 0.5);
    //FragColor = texture(secondTexture, texCoord);
    //FragColor = vec4(0.8, 0.5, 0.2, 1.0);
}